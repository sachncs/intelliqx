{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GoalReceived",
  "description": "Emitted when a new goal is submitted to the platform.",
  "type": "object",
  "required": ["goal_id", "tenant_id", "kind", "description"],
  "properties": {
    "goal_id": {"type": "string"},
    "tenant_id": {"type": "string"},
    "kind": {"type": "string"},
    "description": {"type": "string"},
    "inputs": {"type": "object"},
    "constraints": {"type": "object"}
  }
}